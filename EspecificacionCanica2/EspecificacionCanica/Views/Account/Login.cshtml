@using EspecificacionCanica.Models
@model LoginViewModel

<h2>@ViewBag.Title</h2>

  <div  style="align-content:center; margin-left:43%!important">
    <section id="loginForm">
      @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
      {
          @Html.AntiForgeryToken()

          <div class="row" style="padding-top:65%!important;padding-left:45%!important">
            <div class="col-md-10">
              @Html.LabelFor(m => m.Usuario, new { @class = "control-label" })
            </div>
          </div>
          <div class="row" style="padding-left:20%!important">
            <div class="col-md-10">
              @Html.TextBoxFor(m => m.Usuario, new { @class = "form-control" })
            </div>
          </div>
          <div class="row"  style="padding-left:42%!important">
            <div class="col-md-10" >
              @Html.LabelFor(m => m.Contrasenia, new { @class = "control-label" })

            </div>
          </div>
          <div class="row"  style="padding-left:20%!important">
            <div class="col-md-10"> 
              @Html.PasswordFor(m => m.Contrasenia, new { @class = "form-control" })
            </div>
          </div>

          
          @*<div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <div class="checkbox">
                        @Html.CheckBoxFor(m => m.RememberMe)
                        @Html.LabelFor(m => m.RememberMe)
                    </div>
                </div>
            </div>*@
          <div class="row" style="align-content:center; padding-left:40%!important">
            <div class="col-md-12">
              <input type="submit" value="ENTRAR" class="btn btn-primary" style="height:101px;width:101px; margin-top:15%; border-radius:100%;margin-left:-3%;font-size:12pt;font-weight:bold;" />

            </div>
          </div>


                @* Habilite esta opción después de habilitar la confirmación de la cuenta para la función de restablecimiento de contraseña
                  <p>
                      @Html.ActionLink("¿Ha olvidado su contraseña?", "ForgotPassword")
                  </p>*@
      }
    </section>
  </div>


@section Scripts {
  @Scripts.Render("~/bundles/jqueryval")
}